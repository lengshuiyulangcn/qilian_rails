<% content_for :extra_head do %>
  <title>七联就职资讯-有态度的资讯，左右职业人生|七联就职</title>
<% end %>
<!-- Page Content -->
<div class="container" id="events-detail">
  <!-- Page Heading/Breadcrumbs -->
  <div class="row">
    <div class="col-lg-12">
       <br>
       <ol class="breadcrumb">
         <li><%=link_to "Home", root_path%></li>
         <li><%=link_to "活动一览", events_list_path%></li>
         <li><%=@event.title%></li>
       </ol>
      </div>
  </div>
  <!-- /.row -->

  <!-- Content Row -->
  <div class="row">
    <!-- Post Content Column -->
    <div class="col-lg-8">
      <!-- Post -->
      
      
      <!-- Post Content Markdown Editor -->
    <div class="panel panel-default" style="margin:0px;">
    <div class="panel-heading">
      <!-- or we can add slides show here -->
      <%= image_tag @event.image, class: "img-responsive" %>
    </div>
    <div class="panel-body">
      <div class="col-md-12">
        <h2 style="margin-bottom:5px; padding-bottom:5px;"><strong><%= @event.title %></strong></h2>
      </div>
      <div class="col-md-12">
        <p>
        <%= @event.description %>
        </p>
      </div>
    </div><!-- end of panel-body -->
    <!-- Events info for table -->
    <table class="table table-striped table-bordered">
      <tr>
        <td width="20%"><i class="fa fa-stop"> 讲座内容</td>
        <td>  
          <%== @event.content %>	
        </td>
      </tr>                  
      <tr>
        <td><i class="fa fa-stop"> 日期</td>
        <td><%=@event.timestart.strftime("%y/%m/%d %H:%M")+" ~ "+@event.timeend.strftime("%H:%M")%></td>
      </tr>
      <tr>
        <td><i class="fa fa-stop"> 场地</td>
        <td>東京都文京区音羽１丁目８−８ THE NEWORLD BLD. 4F(地铁5号口出门左转，直走)</td>
      </tr>
      <tr>
        <td><i class="fa fa-stop"> 活动参加人数</td>
        <td><%=@event.users.length.to_s+"/"+@event.limit.to_s%></td>
      </tr>
      <tr>
        <td><i class="fa fa-stop"></i> 费用</td>
        <td><%=@event.fee%></td>
      </tr>                                                                                          
    </table>
    </div><!-- end of Panel -->
      <br>
      <div class="col-md-8 col-md-offset-2 col-xs-12">
        <% if Time.now < @event.timestart %>
          <%=form_tag ('/event/apply') do%>
            <%= hidden_field_tag :event_id, @event.id %>
            <%= submit_tag "立即报名", class: "btn btn-success btn-block btn-event_detail" %><br>
          <% end %>
        <% end %>
      </div>
  </div>
      <!-- main-right sidebar -->
      <div class="col-lg-4">
        <%= render partial: "common/event_side" %>
    </div>
    <!-- /main-right sidebar -->
  </div>
</div>

