<div class="pull-right">
  <i class="fa fa-plus-square"></i><%=link_to "添加课程", new_course_path, class: 'btn btn-blue'%>
</div>
<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th class="col-md-5">课程情况</th>
      <th class="col-md-2">报名情况</th>
      <th>创建时间</th>
      <th>操作</th>
    </tr>
  </thead>
  <tbody>
    <% @courses.each do |course| %>
    <tr>
      <td>
        <h5><strong><%=link_to course.name, course_path(course.id) %></strong></h5>
      </td>
      <td>0/30(hard_coded)</td>
      <td><%= course.created_at %></td>
      <td>
        <ul class="list-inline">
          <li>
          <%=link_to "修改", edit_course_path(course), class: 'btn btn-blue'%>
          </li>
          <li>
          <%=link_to "删除", course_path(course), method: 'delete', class: 'btn btn-blue'%>
            <?php
          </li>
        </ul>
        
      </td>
    </tr>
  <% end %>
  </tbody>
</table> 
